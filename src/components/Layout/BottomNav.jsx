
import { Home, Target, CheckCircle2, Settings } from "./Icons";

export default function BottomNav({ page, setPage }) {
  return (
    <div className="glass-bottom-nav fixed bottom-0 left-0 right-0 max-w-lg mx-auto">
      {/* paste your existing bottom nav JSX here */}
    </div>
  );
}
